// import { SELECTION } from "../actions";

// export const update_selection = (dispatch) => next => action => {
//     next(action)

//     if (action.type === SELECTION) {
//       dispatch(select_category({}))
//     }
//   }
