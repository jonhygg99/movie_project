// import { PREVIOUS_PAGE, previous_page, next_page, NEXT_PAGE, INITIAL_PAGE, initial_page } from '../actions'

// export const update_page = (dispatch) => (next) => (action) => {
// 	next(action)
// 	if (action.type === PREVIOUS_PAGE) {
// 		dispatch(previous_page())
// 	} else if (action.type === NEXT_PAGE) {
// 		dispatch(next_page({ max_page: action.payload }))
// 	} else if (action.type === INITIAL_PAGE) {
// 		dispatch(initial_page())
// 	}
// }
